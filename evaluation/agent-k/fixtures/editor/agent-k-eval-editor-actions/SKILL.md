---
name: agent-k-eval-editor-actions
description: Use the deterministic Agent K evaluation Editor actions for palette, todo, outline, timeline, key-value, bookmark, inventory, route, snippet, and score fixture files.
---

# Agent K evaluation Editor actions

Use the action declared for the active Editor in the injected Agent K file-format context. Call `agent_k` with capability `file-editor`, copy the declared action exactly, and do not replace the Editor action with a filesystem edit.
