---
name: agent-k-video-editor
description: Control video currently open in Agent K. Use when Agent K supplies an active video-editor context with play, pause, or seek capabilities.
---

# Agent K Video Player

Read the active path and allowed actions from `<agent_k_file_format>`. Use `agent_k_file_editor` only with an advertised action. For `seek`, pass `seconds`; positive values move forward and negative values move backward.
