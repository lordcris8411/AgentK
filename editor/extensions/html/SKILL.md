---
name: agent-k-html-editor
description: Work with HTML currently open in Agent K, including source and sandboxed preview. Use when Agent K supplies an active HTML-editor context.
---

# Agent K HTML Editor

Agent K may add an `<agent_k_file_format>` block containing the active HTML path. Use Pi's normal read, edit, and write tools for source changes. Agent K owns the sandboxed preview and refresh controls.
